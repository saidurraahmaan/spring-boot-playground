package com.dsi.dms.web.config;

import java.util.Locale;

public final class AppConstants
{
    public static final String DEFAULT_ENCODING = "UTF-8";
    public static final Locale BANGLA_LOCALE = new Locale.Builder().setLanguage("bn").setRegion("BD").build();
    public static final Locale ENGLISH_LOCALE = new Locale.Builder().setLanguage("en").setRegion("US").build();

}
